import {createClient} from '@/lib/supabase/server';export const metadata={title:'Saved Content'};export default async function Page(){const s=await createClient();let items:any[]=[];if(s){const {data:{user}}=await s.auth.getUser();if(user){const r=await s.from('saved_content').select('id,content_type,content_slug,created_at').order('created_at',{ascending:false});items=r.data||[]}}return <main className="section"><div className="container"><div className="eyebrow">Your library</div><h1>Saved for later.</h1><p className="lead">Keep recipes, articles, videos and product guides together.</p><div className="grid3">{items.length?items.map(i=><div className="card" key={i.id}><h3>{i.content_slug}</h3><p>{i.content_type}</p></div>):<div className="card"><h3>Your collection is ready.</h3><p>Save buttons can write to the Supabase <code>saved_content</code> table after sign-in.</p></div>}</div></div></main>}
