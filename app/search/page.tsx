export const metadata={title:'Search'};export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const p=await searchParams;return <main className="section"><div className="container"><div className="eyebrow">Search</div><h1>Find something wonderful.</h1><form><input className="field" name="q" placeholder="Search recipes, videos, articles..." defaultValue={p.q}/><button className="pill primary">Search</button></form><div className="card" style={{marginTop:35}}><h3>{p.q?`Results for “${p.q}”`:'Popular right now'}</h3><p>The beta search interface is ready. Connect published content in Supabase to return live results.</p></div></div></main>}
