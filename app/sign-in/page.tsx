import Link from 'next/link';import {signIn} from '@/lib/auth-actions';export const metadata={title:'Sign In'};export default async function Page({searchParams}:{searchParams:Promise<{message?:string}>}){const p=await searchParams;return <main><form className="auth" action={signIn}><div className="eyebrow">Member account</div><h1 className="serif" style={{fontSize:48,color:'#073f2a'}}>Welcome back.</h1>{p.message&&<div className="notice">{p.message}</div>}<label>Email<input className="field" name="email" type="email" required/></label><label>Password<input className="field" name="password" type="password" minLength={8} required/></label><button className="pill primary" type="submit">Sign in</button><p>New here? <Link href="/sign-up" style={{fontWeight:800}}>Create an account</Link></p></form></main>}
