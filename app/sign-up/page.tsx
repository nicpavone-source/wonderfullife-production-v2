import Link from 'next/link';import {signUp} from '@/lib/auth-actions';export const metadata={title:'Join'};export default async function Page({searchParams}:{searchParams:Promise<{message?:string}>}){const p=await searchParams;return <main><form className="auth" action={signUp}><div className="eyebrow">Join WonderfulLife</div><h1 className="serif" style={{fontSize:48,color:'#073f2a'}}>Begin your journey.</h1>{p.message&&<div className="notice">{p.message}</div>}<label>Email<input className="field" name="email" type="email" required/></label><label>Password<input className="field" name="password" type="password" minLength={8} required/></label><button className="pill primary" type="submit">Create account</button><p>Already a member? <Link href="/sign-in" style={{fontWeight:800}}>Sign in</Link></p></form></main>}
