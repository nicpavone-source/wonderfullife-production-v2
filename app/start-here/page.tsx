import Link from 'next/link';const goals=['More Energy','Better Sleep','Healthy Nutrition','Less Stress','Move More','Healthy Aging'];export const metadata={title:'Start Here'};export default function Page(){return <main className="section"><div className="container"><div className="eyebrow">Your first step</div><h1>What would you like help with today?</h1><p className="lead">Choose a goal. Zoey will help turn it into a friendly, realistic starting plan.</p><div className="grid3">{goals.map(g=><Link className="card" href={`/ask-zoey?goal=${encodeURIComponent(g)}`} key={g}><h3>{g}</h3><p>Build a simple path with practical suggestions, recommended content and one achievable action for today.</p><strong style={{color:'#075c3a'}}>Create my path →</strong></Link>)}</div></div></main>}
